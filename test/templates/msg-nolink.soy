/**
 * A msg command template.
 * @param {string} name
 * @param {int} age
 */
{template test.templates.MsgNoLinkCommand}
{msg meaning="intro" desc="Intro&duction"}I am {print $name}, {print $age} years old.{/msg}
{/template}
