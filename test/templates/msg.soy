/**
 * A msg command template.
 * @param {string} name
 * @param {int} age
 */
{template test.templates.MsgCommand}
{msg meaning="intro" desc="Intro&duction"}I am {print $name}, {print $age} years old. Check out my <a href="#">profile</a> and <a href="{print $website}">website</a>.<br>What's your name?{/msg}
{/template}
